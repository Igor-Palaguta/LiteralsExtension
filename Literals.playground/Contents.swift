import UIKit

let red = UIColor(red: 1, green: 0, blue: 0, alpha: 1)
let green = UIColor.init(red: 0, green: 1, blue: 0, alpha: 1)

let blue = UIColor(red: 0,
                   green: 0,
                   blue: 1,
                   alpha: 1)

let colors = [UIColor(red: 0.5, green: 0.5, blue: 0.5, alpha: 0.5), UIColor(red: 0, green: 0, blue: 0, alpha: 1)]

//let nsBlack = NSColor(red: 0, green: 0, blue: 0, alpha: 1)

let dnipro = UIImage(named: "FCDnipro.png")
let bacelona = UIImage.init(named: "FCBarcelona")

let images = [UIImage(named: "FCDnipro"), UIImage(named: "FCBarcelona")]
